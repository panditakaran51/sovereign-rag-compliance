[pytest]
testpaths = tests
asyncio_mode = auto

markers =
    integration: API-level tests that require the FastAPI app (no LLM/Qdrant needed)
    requires_services: tests that need a live Qdrant + Ollama stack
